spring:
  cloud:
    aws:
      access-key: test
      secret-key: test
      region:
        static: eu-west-1
      s3:
        # temporarily disables s3 autoconfiguration set me to true
        enabled: true
        endpoint: http://localhost:4566/